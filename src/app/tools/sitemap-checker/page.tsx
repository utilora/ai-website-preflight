import { ToolLanding, toolPageMetadata } from "../tool-landing";

export const metadata = toolPageMetadata("sitemap-checker");

export default function Page() {
  return <ToolLanding id="sitemap-checker" />;
}
