import { ToolLanding, toolPageMetadata } from "../tool-landing";

export const metadata = toolPageMetadata("security-headers-checker");

export default function Page() {
  return <ToolLanding id="security-headers-checker" />;
}
