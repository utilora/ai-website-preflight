import { ToolLanding, toolPageMetadata } from "../tool-landing";

export const metadata = toolPageMetadata("meta-tag-checker");

export default function Page() {
  return <ToolLanding id="meta-tag-checker" />;
}
