import { ToolLanding, toolPageMetadata } from "../tool-landing";

export const metadata = toolPageMetadata("robots-txt-checker");

export default function Page() {
  return <ToolLanding id="robots-txt-checker" />;
}
