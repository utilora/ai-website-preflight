import { ToolLanding, toolPageMetadata } from "../tool-landing";

export const metadata = toolPageMetadata("open-graph-checker");

export default function Page() {
  return <ToolLanding id="open-graph-checker" />;
}
