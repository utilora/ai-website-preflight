import { ToolLanding, toolPageMetadata } from "../tool-landing";

export const metadata = toolPageMetadata("broken-link-checker");

export default function Page() {
  return <ToolLanding id="broken-link-checker" />;
}
