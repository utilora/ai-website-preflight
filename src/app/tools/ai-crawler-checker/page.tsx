import { ToolLanding, toolPageMetadata } from "../tool-landing";

export const metadata = toolPageMetadata("ai-crawler-checker");

export default function Page() {
  return <ToolLanding id="ai-crawler-checker" />;
}
