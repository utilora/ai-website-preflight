# Documentation

- `requirements/` contains source-of-truth product requirements converted from the supplied Word document.
- `tasks/` contains the supplied phase task briefs, including Phase 05.5 hardening, Phase 06 tools, and Phase 07 production deployment.
- `architecture/` and `decisions/` describe the current single-process Next.js layout and later hardening/deploy choices.
- `deployment/production.md` is the operator runbook for a single 1 GB VPS.
- `reviews/` is reserved for human acceptance records.
